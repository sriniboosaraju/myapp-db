apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-db-dev-infra
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/sriniboosaraju/myapp-db.git'
    targetRevision: dev-infra
    path: charts/myapp-db
    helm:
      valueFiles:
        - values-dev-infra.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: myapp-db-dev-infra
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
