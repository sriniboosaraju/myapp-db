apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-db-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/sriniboosaraju/myapp-db.git'
    targetRevision: HEAD
    path: charts/myapp-db
    helm:
      valueFiles:
        - values-staging.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: myapp-db-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
